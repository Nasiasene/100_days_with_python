+ HTML = esqueleto do site (FUNDAMENTAL).

+CSS = estiliza o site.

+JavaScrp = o funcionamento por tras do site.


-HTML: HyperText Markup Language 
HyperText: texto que te leva para outros textos/sites.
Markup Language: Tags que caracterizam se aquele texto é oridnario, ou é um cabeçalho, etc.

ex: <h1>TEXT</h1> //This a heading
<h1> e </h1> são tags.
<h1>TEXT</h1> é um elemento.

<h1>heading</h1> 
<p>paragraph</p>


void Elements: não tem nenhum tipo de conteúdo pos sua tag.

<hr />   or <hr> //Horizontal Rule (literal line)
<br />   or <br> //Break (\n)
