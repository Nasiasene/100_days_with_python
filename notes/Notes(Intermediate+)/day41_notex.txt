+ HTML = site skeleton (FUNDAMENTAL).

+CSS = styles the site.

+JavaScrp = the workings behind the website.


-HTML: HyperText Markup Language
HyperText: text that takes you to other texts/sites.
Markup Language: Tags that characterize whether that text is original, or is a heading, etc.

ex: <h1>TEXT</h1> //This a heading
<h1> and </h1> are tags.
<h1>TEXT</h1> is an element.

<h1>heading</h1>
<p>paragraph</p>


void Elements: does not have any type of content after its tag.

<hr /> or <hr> //Horizontal Rule (literal line)
<br /> or <br> //Break (\n)